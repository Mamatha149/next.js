// app/page.js (or .tsx for TypeScript)
import Link from 'next/link'; // Import the Link component for client-side navigation

const HomePage = () => {
  return (
    <div>
      <h1>Welcome to Next.js!</h1>
      <p>This is a simple Next.js application using the App Router.</p>
      {/* Use the Link component for navigation to other pages */}
      <Link href="/about">
        {/* In newer Next.js versions, the <a> tag is automatically included inside <Link> */}
        Go to About Page
      </Link>
    </div>
  );
};

export default HomePage;
